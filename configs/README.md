# Configs

Versioned experiment configs live here.

```text
data/    dataset audit and split configs
lewm/    cross-platform LeWM model-tier metadata only
qwen/    Qwen baseline configs
router/  router and AdapterCard registry configs
eval/    comparison/evaluation configs
```

Linux/Hydra runtime configs for actual LeWM training live under `wsl/lewm/configs/`.
